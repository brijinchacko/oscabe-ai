import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Hire Automation Engineers UK | Pre-Screened PLC & Controls Talent | OSCABE",
  description:
    "Access verified PLC programmers, SCADA engineers, and controls specialists. AI-powered matching with 48-hour shortlists. From £1,500 flat fee.",
};

export default function EmployersLayout({ children }: { children: React.ReactNode }) {
  return children;
}
