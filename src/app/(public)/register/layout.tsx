import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Register as Automation Engineer | OSCABE Talent Pool | UK",
  description:
    "Join OSCABE's verified talent pool. Upload your CV, get skill-verified, and access premium PLC, SCADA, and controls engineering roles.",
};

export default function RegisterLayout({ children }: { children: React.ReactNode }) {
  return children;
}
