import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Automation Engineer Jobs UK | PLC, SCADA & Controls Vacancies | OSCABE",
  description:
    "Find your next PLC, SCADA, or controls engineering role. Get skill-verified and matched with top UK employers. Free registration.",
};

export default function CandidatesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
