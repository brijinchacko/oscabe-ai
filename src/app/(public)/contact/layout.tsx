import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact OSCABE | Automation Recruitment Enquiries | UK",
  description:
    "Get in touch with OSCABE for automation recruitment. Milton Keynes, UK. Employers, candidates, and agency partners welcome.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
